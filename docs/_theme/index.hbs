---
layout: docs
title: Mapbox GL JS API
navigation:
{{#each docs}}
  - title: {{name}}
    url: /api
    id: {{permalink}}
    subnav:
    {{#each members.static}}
      - title: .{{name}}
        url: /api
        id: {{permalink}}
    {{/each}}
    {{#each members.instance}}
      - title: '#{{name}}'
        url: /api
        id: {{permalink}}
    {{/each}}
{{/each}}
---
{{#each docs ~}}{{> section}}{{/each}}
