{
  "name": "vite-build-test",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build": "./pretest.sh && vite build",
    "test": "npm run build && node --test"
  },
  "devDependencies": {
    "vite": "^8.1.4"
  }
}
