{
  "name": "feature-filter",
  "version": "0.0.0",
  "main": "./index.js",
  "description": "Creates filtering function for vector tile features",
  "dependencies": {
  },
  "devDependencies": {
    "tape": "2.13.x"
  },
  "scripts": {
    "test": "tape test.js"
  },
  "engines": {
    "node": "0.10.x"
  }
}
