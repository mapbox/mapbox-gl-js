{
  "name": "llmr-gl",
  "description": "A WebGL interactive maps library",
  "version": "0.0.1",
  "main": "js/llmr.js",
  "dependencies": {
    "geojson-rewind": "~0.1.0",
    "rbush": "~1.3.2",
    "pbf": "0.0.1",
    "unitbezier": "0.0.0",
    "queue-async": "^1.0.7"
  },
  "devDependencies": {
    "benchmark": "~1.0.0",
    "browserify": "~3.46.0",
    "spritesmith": "~0.17.4",
    "uglify-js": "~2.4.12",
    "beefy": "https://github.com/chrisdickinson/beefy/archive/beefy-two.tar.gz",
    "glify": "^0.2.0",
    "tape": "~2.12.1",
    "watchify": "~0.8.3",
    "jshint": "^2.5.0",
    "hintify": "^0.1.0"
  },
  "browserify": {
    "transform": [
      "hintify",
      "glify"
    ]
  },
  "scripts": {
    "test": "tape test/*.js",
    "start": "beefy -t glify debug/site.js -d",
    "watch": "watchify -v -d js/llmr.js -o dist/llmr-dev.js",
    "build": "browserify -d js/llmr.js > dist/llmr-dev.js",
    "production": "browserify -i js/render/draw{vertices,debug}.js -d js/llmr.js | uglifyjs -c drop_console=true > dist/llmr.js",
    "prepublish": "mkdir -p dist && npm run build && npm run production"
  },
  "testling": {
    "files": "test/*.js",
    "browsers": [
      "chrome/22..latest",
      "firefox/16..latest",
      "safari/latest"
    ]
  }
}
